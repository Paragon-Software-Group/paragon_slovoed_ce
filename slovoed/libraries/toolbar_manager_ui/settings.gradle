apply from: '../../gradle/local_build_settings.gradle'

include ':lib'

slovoedProjects (
        'toolbar_manager_api',
        'utils_slovoed_ui'
)

slovoedProjects (
        'dictionary_manager_api',
        'settings_manager_api'
)
